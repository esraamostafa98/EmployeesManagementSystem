@using Microsoft.AspNetCore.Mvc.Localization;
@using Microsoft.Extensions.Localization
@using WebApplication1

@inject IStringLocalizer<SharedResource> SharedLocalizer
@*@inject IHtmlLocalizer<SharedResource> SharedLocalizer*@
@section SelectLink{
    <link href="~/plugins/bootstrap-select/css/bootstrap-select.css" rel="stylesheet" />
}

@model IQueryable<WebApplication1.Models.EmployeeVM>
@{
    ViewBag.Title = SharedLocalizer["Employee"];
    var Culture = System.Threading.Thread.CurrentThread.CurrentCulture.Name;
   
}

@if (Culture == "ar")
{
    <a asp-controller="Employee" asp-action="Index" asp-route-culture="en-US">@SharedLocalizer["English"]</a>
}
@if (Culture == "en-US")
{
    <a asp-controller="Employee" asp-action="Index" asp-route-culture="ar">@SharedLocalizer["Arabic"]</a>
}


<br />

@*<h2>@SharedLocalizer["WTE"]</h2>*@

<a asp-controller="Employee" asp-action="Create" class="btn btn-primary">@SharedLocalizer["AddEmployee"]</a>
<br />
<br />
@*<table class="table table-striped table-bordered text-center">
    <tr>

        <th class="text-center">@SharedLocalizer["Name"]</th>
        <th class="text-center">@SharedLocalizer["Salary"]</th>
        <th class="text-center">@SharedLocalizer["Email"]</th>
        <th class="text-center">@SharedLocalizer["Address"]</th>
        <th class="text-center">@SharedLocalizer["HireDate"]</th>
        <th class="text-center">@SharedLocalizer["District"]</th>
        <th class="text-center">@SharedLocalizer["Department"]</th>
        <th class="text-center">@SharedLocalizer["Notes"]</th>
        <th class="text-center">@SharedLocalizer["Edit"]</th>
        <th class="text-center">@SharedLocalizer["Delete"]</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>

            <td>@item.Name</td>
            <td>@item.Salary</td>
                <td>@item.Email</td>
            <td>@item.Address</td>
            <td>@item.HireDate.Date.ToShortDateString()</td>
            <td>@item.DistrictName</td>
            <td>@item.DepartmentName</td>
                <td>@item.Note</td>
            <td><a asp-controller="Employee" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-success">@SharedLocalizer["Edit"]</a></td>
            <td><a asp-controller="Employee" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">@SharedLocalizer["Delete"]</a></td>

        </tr>
    }


</table>*@


<div class="container-fluid">
    <div class="row clearfix">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <div class="card">
                <div class="header">
                    <h2>@SharedLocalizer["WTE"]</h2>
                    <ul class="header-dropdown m-r--5">
                        <li class="dropdown">
                            <a href="javascript:void(0);" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                                <i class="material-icons">more_vert</i>
                            </a>
                            <ul class="dropdown-menu pull-right">
                                <li><a href="javascript:void(0);">Action</a></li>
                                <li><a href="javascript:void(0);">Another action</a></li>
                                <li><a href="javascript:void(0);">Something else here</a></li>
                            </ul>
                        </li>
                    </ul>
                </div>
                <div class="body">
                    <div class="table-responsive">
                        <table class="table table-bordered table-striped text-center table-hover js-basic-example dataTable">
                            
                            <thead>
                                <tr>

                                    <th class="text-center">@SharedLocalizer["Name"]</th>
                                    <th class="text-center">@SharedLocalizer["Salary"]</th>
                                    <th class="text-center">@SharedLocalizer["Photo"]</th>
                                    <th class="text-center">@SharedLocalizer["CV"]</th>
                                   @* <th class="text-center">@SharedLocalizer["Email"]</th>
                                    <th class="text-center">@SharedLocalizer["Address"]</th>*@
                                    @*<th class="text-center">@SharedLocalizer["HireDate"]</th>
                                    <th class="text-center">@SharedLocalizer["District"]</th>*@
                                    @*<th class="text-center">@SharedLocalizer["Department"]</th>*@
                                   @* <th class="text-center">@SharedLocalizer["Notes"]</th>*@
                                    <th class="text-center">@SharedLocalizer["Edit"]</th>
                                    <th class="text-center">@SharedLocalizer["Delete"]</th>
                                    <th class="text-center">@SharedLocalizer["Details"]</th>

                                </tr>
                            </thead>
                           <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>

                                        <td>@item.Name</td>
                                        <td>@item.Salary</td>
                                        <td><img src="~/Files/Photos/@item.PhotoName" width="30px" height="30px" /></td>
                                        <td><a href="~/Files/Docs/@item.CVName">Download File</a></td>
                                       @* <td>@item.Email</td>
                                        <td>@item.Address</td>*@
                                      @*  <td>@item.HireDate.Date.ToShortDateString()</td>
                                        <td>@item.DistrictName</td>
                                        <td>@item.DepartmentName</td>*@
                                      @*  <td>@item.Note</td>*@
                                        <td><a asp-controller="Employee" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-success">@SharedLocalizer["Edit"]</a></td>
                                        <td><a asp-controller="Employee" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger">@SharedLocalizer["Delete"]</a></td>
                                        <td><a asp-controller="Employee" asp-action="Details" asp-route-id="@item.Id" class="btn btn-primary">@SharedLocalizer["Details"]</a></td>
                                    </tr>
                                }
                           </tbody>
                         
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



>
@section DataTableSection{
<script>
    $(function () {
        $('.js-basic-example').DataTable({
            responsive: true,
            paging: true,
            pageLength: 5,
            bSort: true,
            bFilter: true,
            //bLengthChange: false,
            //bInfo: false,
            lengthMenu: [[5, 10, 25, 50, -1], [5, 10, 25, 50, "All"]],

        });
    });

</script>
}